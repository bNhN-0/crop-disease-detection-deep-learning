# Models Directory

This directory stores trained model files used for evaluation and inference.

Included model:

- `best_model.keras` → final selected EfficientNetB0 model used by the Streamlit application

Training checkpoints and additional experimental models are not committed to the repository.

Expected training model files:

- `crop_disease_mobilenetv2_1.keras`
- `crop_disease_efficientnetb0_2.keras`

The deployed inference model:

- `best_model.keras`